<div>
  <div class="contextual">
    <%= link_to 'New', new_admin_mail_ticket_provider_path, class: 'icon icon-add' %>
  </div>

  <% if @mail_providers.any? %>
    <div id="configure-data">
      <h3>Mail Providers</h3>
      <table class="list">
        <thead>
          <tr>
            <th>Provider</th>
            <th>Status</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% @mail_providers.each do | mail_provider | %>
            <tr>
              <td><%= mail_provider.name.capitalize %></td>
              <td><%= mail_provider.active  ? 'Active' : 'Inactive' %></td>
              <td>
                <%= link_to 'Edit', edit_admin_mail_ticket_provider_path(mail_provider), class: "icon icon-edit" %> |
                <%= link_to 'Delete', admin_mail_ticket_provider_path(mail_provider), method: :delete, class: "icon icon-del"  %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <p>No mail provider found.</p>
  <% end %>
</div>